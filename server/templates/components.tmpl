{{ define "counter" }}
<button>
    Current value: $count
    <lua>
        count = 0

        function on_submit()
            count = count + 1
        end

        function pre_content()
            if count >= 10 then
                self.palette = "error"
            end
        end
    </lua>
</button>
{{ end }}
